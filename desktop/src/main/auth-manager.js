// Auth Manager — login, logout, Google OAuth, org selection (H2 refactor)
// Stub module — auth logic remains in index.js for now due to deep coupling
// with IPC handlers. This file serves as the extraction target for future work.

const AppState = require('./app-state');

module.exports = {
  // Placeholder — auth logic is still in index.js
  // Future: extract login, logout, Google OAuth, org selection handlers here
};
